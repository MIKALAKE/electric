// Public Paths
const LANDING_PAGE = '/';

const NOT_FOUND = '/not-found';

// Private Paths

const publicPaths = {
  DEFAULT_PATH: LANDING_PAGE,
  LANDING_PAGE,
  NOT_FOUND,
};

export default { public: publicPaths };
